
spring:
  application:
    name: app

eureka:
  instance:
    # 指定该Eureka实例的主机名
    hostname: localhost
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:1112/eureka/